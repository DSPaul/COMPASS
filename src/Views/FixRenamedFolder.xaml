<Page x:Class="COMPASS.Views.FixRenamedFolder"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:COMPASS.Views"
      xmlns:Tools="clr-namespace:COMPASS.Tools"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="FixRenamedFolder">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Fix Renamed Folder" Grid.Row="0" Margin="10,5"/>
        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <TextBox Tools:AP.PlaceHolderText="old\folder\path\" 
                     Width="200" BorderThickness="1"/>
            <TextBlock Text="To"/>
            <TextBox Tools:AP.PlaceHolderText = "new\folder\path\" Width="200"/>
        </StackPanel>

    </Grid>
</Page>
